const { czech } = require('./czech');
const { english } = require('./english')
const { french } = require('./french');
const { italian } = require('./italian');
const { portuguese } = require('./portuguese')
const { spanish } = require('./spanish');

const wordlist = {
    czech,
    english,
    french,
    italian,
    portuguese,
    spanish
}

module.exports = wordlist;
